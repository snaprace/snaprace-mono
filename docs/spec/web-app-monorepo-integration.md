# SnapRace Web App 모노레포 통합 계획

## 개요

SnapRace Next.js 애플리케이션(`apps/web`)을 기존 turborepo 모노레포 환경에 완전히 통합하기 위한 구현 계획입니다.

**작성일**: 2025-11-01  
**상태**: 실행 대기 중  
**배포**: Vercel (별도 설정)

---

## 1. 현재 상황 분석

### 1.1 모노레포 환경

- **모노레포 도구**: Turborepo v2.5.8
- **패키지 매니저**: pnpm v9.0.0 (workspace 사용)
- **Node 버전 요구사항**: >= 18

### 1.2 기존 앱 구조

```
apps/
├── docs/          # 샘플 Next.js 앱 (포트 3001)
├── infra/         # AWS CDK 인프라
└── web/           # SnapRace 메인 애플리케이션 (새로 추가됨)

packages/
├── eslint-config/ # 공유 ESLint 설정
├── typescript-config/ # 공유 TypeScript 설정
└── ui/            # 공유 UI 컴포넌트

previous-lambda-functions/
└── *.py           # 기존 Lambda 함수 (추후 작업 예정)
```

### 1.3 apps/web 특성

- **Next.js**: v15.2.3 (App Router)
- **React**: v19.0.0
- **주요 기술 스택**:
  - tRPC (서버-클라이언트 통신)
  - Next-Auth v5 beta (인증)
  - AWS SDK (DynamoDB, S3)
  - Tailwind CSS v4.0
  - React Query (데이터 페칭)
  - Vitest (테스트)

- **패키지 매니저**: pnpm v10.8.0 (루트보다 최신)
- **포트**: 3000 (기본)
- **배포**: Vercel (사용자가 직접 설정 예정)

---

## 2. 통합 목표

### 2.1 개발 환경 통합 (핵심)

✅ **목표**: 모노레포 루트에서 `pnpm dev` 실행 시 apps/web도 함께 시작  
✅ **목표**: 모든 워크스페이스 패키지가 정상적으로 연결  
✅ **목표**: 타입 체크 및 린팅 통합

### 2.2 빌드 파이프라인 통합 (핵심)

✅ **목표**: `pnpm build` 실행 시 의존성 순서대로 빌드  
✅ **목표**: Turbo 캐싱 활용하여 빌드 최적화  
✅ **목표**: Vercel 배포 시 모노레포 환경 인식

---

## 3. 구현 단계

### Phase 1: 패키지 의존성 정리 (우선순위: 높음)

#### 3.1 패키지 매니저 버전 통일

**현재 문제**:

- 루트: `pnpm@9.0.0`
- apps/web: `pnpm@10.8.0`

**해결 방안**:

```json
// 루트 package.json
{
  "packageManager": "pnpm@10.8.0"
}
```

**작업 항목**:

- [ ] 루트 package.json의 packageManager 필드 업데이트
- [ ] 전체 프로젝트 재설치 (`pnpm install`)
- [ ] .npmrc 설정 확인

#### 3.2 공유 패키지 활용 검토

**apps/web이 독립적으로 관리하는 항목**:

- ESLint 설정
- TypeScript 설정
- UI 컴포넌트 (많은 shadcn/ui 컴포넌트)

**공유 가능 여부 판단**:

| 항목              | 공유 여부    | 이유                                 |
| ----------------- | ------------ | ------------------------------------ |
| eslint-config     | ❌ 독립 유지 | Next.js 15 특화 설정 필요            |
| typescript-config | ⚠️ 부분 공유 | 기본 설정은 공유, web 전용 확장      |
| ui 컴포넌트       | ❌ 독립 유지 | apps/web만 사용, Tailwind 4.0 의존성 |

**작업 항목**:

- [ ] apps/web에서 TypeScript 설정 확인
- [ ] @repo/typescript-config를 extends하도록 수정 (선택)
- [ ] 나머지는 apps/web 내부에서 독립 관리

### Phase 2: Turborepo 통합 (우선순위: 높음)

#### 2.1 apps/web package.json 수정

**변경 사항**:

```json
{
  "name": "@repo/web", // workspace naming convention
  "scripts": {
    "build": "next build",
    "dev": "next dev --turbo",
    "lint": "next lint",
    "check-types": "tsc --noEmit", // turbo.json과 일치
    "test": "vitest"
  }
}
```

**작업 항목**:

- [ ] package name을 `@repo/web`으로 변경
- [ ] scripts 명령어 정규화
- [ ] 불필요한 스크립트 제거 (format:\*, preview 등)

#### 2.2 turbo.json 업데이트

**추가할 작업**:

```json
{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "**/*.test.{ts,tsx}"],
      "outputs": ["coverage/**"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    }
  }
}
```

**작업 항목**:

- [ ] test 태스크 추가 (apps/web용)
- [ ] 환경 변수 입력 명시 (.env\*)
- [ ] 캐시 전략 최적화

#### 2.3 루트 package.json 스크립트 추가

```json
{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "dev:web": "turbo run dev --filter=@repo/web",
    "build:web": "turbo run build --filter=@repo/web"
  }
}
```

**작업 항목**:

- [ ] test 스크립트 추가
- [ ] 개별 앱 실행 스크립트 추가 (dev:web, dev:docs 등)

### Phase 3: 환경 변수 관리 (우선순위: 중간)

#### 3.1 현재 apps/web 환경 변수 분석

**필수 환경 변수**:

```bash
# AWS
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# DynamoDB
DYNAMODB_TABLE_PHOTOS=
DYNAMODB_TABLE_EVENTS=
DYNAMODB_TABLE_USERS=

# S3
S3_BUCKET_NAME=

# Auth
NEXTAUTH_SECRET=
NEXTAUTH_URL=
```

#### 3.2 환경별 설정 파일 구조

```
apps/web/
├── .env.example           # 템플릿
├── .env.local.example     # 로컬 개발용 템플릿
├── .env.development       # 개발 환경 (Git 제외)
├── .env.staging           # 스테이징 환경 (Git 제외)
└── .env.production        # 프로덕션 환경 (Git 제외)
```

**작업 항목**:

- [ ] .env.example 파일 검토 및 업데이트
- [ ] .env.local.example 파일 생성
- [ ] README에 환경 변수 설정 가이드 추가
- [ ] .gitignore에 .env\* 추가 확인

#### 3.3 env.js 유효성 검사 유지

apps/web의 `src/env.js`는 `@t3-oss/env-nextjs`를 사용하여 환경 변수 유효성 검사를 수행합니다. 이는 그대로 유지합니다.

**작업 항목**:

- [ ] env.js 파일 검토
- [ ] 필수 변수 누락 여부 확인

### Phase 4: 환경 변수 및 Vercel 설정 준비 (우선순위: 중간)

#### 4.1 Vercel 모노레포 연동 준비

**Vercel 설정 사항**:

- Root Directory: `apps/web`
- Build Command: `pnpm build` (turbo 사용)
- Output Directory: `.next` (기본값)
- Install Command: `pnpm install`

**작업 항목**:

- [ ] vercel.json 파일 생성 (필요시)
- [ ] Vercel 환경 변수 목록 문서화
- [ ] 프로덕션/프리뷰 환경 구분 확인

#### 4.2 AWS 리소스 환경 변수 문서화

**apps/web에서 사용하는 AWS 리소스**:

1. **DynamoDB 테이블**: Photos, Events, Users
2. **S3 버킷**: 사진 저장소
3. **AWS Rekognition**: 얼굴 인식 (Lambda 함수에서 사용)

**작업 항목**:

- [ ] 필수 환경 변수 목록 정리
- [ ] Vercel에 설정할 환경 변수 문서화
- [ ] 개발/프로덕션 환경 구분

#### 4.3 Lambda 함수 통합

**현재 상태**:

- `previous-lambda-functions/`에 Python Lambda 함수들 존재
- 추후 작업 예정 (현재 통합 작업에서 제외)

**작업 항목**:

- [ ] 없음 (추후 별도 작업으로 진행)

### Phase 5: 개발 워크플로우 정리 (우선순위: 낮음)

#### 5.1 README 업데이트

**루트 README**:

```markdown
# SnapRace Monorepo

## Quick Start

\`\`\`bash

# 설치

pnpm install

# 개발 서버 시작 (모든 앱)

pnpm dev

# 특정 앱만 시작

pnpm dev:web
pnpm dev:docs

# 빌드

pnpm build

# 테스트

pnpm test

# 타입 체크

pnpm check-types
\`\`\`
```

**apps/web README**:

- 환경 변수 설정 가이드
- 개발 서버 시작 방법
- 테스트 실행 방법
- 배포 가이드

**작업 항목**:

- [ ] 루트 README 업데이트
- [ ] apps/web README 업데이트
- [ ] CONTRIBUTING.md 작성 (선택)

#### 5.2 Git 설정

**apps/web/.gitignore**:

```
# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
build
dist

# env files
.env*.local
.env.development
.env.staging
.env.production

# testing
coverage
*.lcov

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
```

**작업 항목**:

- [ ] .gitignore 검토 및 업데이트
- [ ] 민감 정보 커밋 이력 확인
- [ ] .env.example 파일만 커밋 확인

#### 5.3 린팅 및 포맷팅 통합

**루트 수준 통합**:

```json
// 루트 package.json
{
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md,json}\""
  }
}
```

**작업 항목**:

- [ ] 루트 prettier 설정 확인
- [ ] apps/web의 prettier-plugin-tailwindcss 유지
- [ ] pre-commit hook 설정 검토 (husky, lint-staged)

### Phase 6: 문서화 및 마이그레이션 (우선순위: 낮음)

#### 6.1 기술 문서 정리

**문서 위치 재구성**:

```
docs/
└── spec/
    ├── web-app-monorepo-integration.md (이 문서)
    ├── lambda-refactoring-analysis.md (기존)
    └── deployment-guide.md (신규)

apps/web/
├── docs/                  # Web 앱 관련 문서
│   ├── event-leaderboard-*.md
│   ├── finishline-video-*.md
│   └── raceroster-*.md
└── tasks/                 # 개발 태스크
    ├── event-leaderboard-*.md
    └── ...
```

**작업 항목**:

- [ ] 문서 위치 정리
- [ ] 배포 가이드 작성
- [ ] 아키텍처 다이어그램 작성 (선택)

#### 6.2 기존 코드 마이그레이션 검토

**previous-lambda-functions/**:

- Python Lambda 함수들이 있음
- apps/web에서 일부 기능 중복 가능

**검토 사항**:

- [ ] Lambda 함수 vs Next.js API Routes 비교
- [ ] 마이그레이션 가능한 함수 식별
- [ ] 성능 및 비용 분석

---

## 4. 구현 체크리스트

### ✅ Phase 1: 패키지 의존성 정리 (필수)

- [ ] 루트 package.json의 pnpm 버전을 10.8.0으로 업데이트
- [ ] 루트에서 `pnpm install` 실행하여 전체 재설치
- [ ] apps/web의 의존성이 정상적으로 설치되는지 확인
- [ ] `pnpm --version` 명령으로 버전 확인

### ✅ Phase 2: Turborepo 통합 (필수)

- [ ] apps/web/package.json의 name을 `@repo/web`으로 변경
- [ ] turbo.json에 `test` 태스크 추가
- [ ] 루트 package.json에 `test`, `dev:web`, `build:web` 스크립트 추가
- [ ] 루트에서 `pnpm dev` 실행하여 모든 앱이 정상 시작되는지 확인
- [ ] 루트에서 `pnpm build` 실행하여 빌드 성공 확인

### ⚠️ Phase 3: 환경 변수 관리 (중요)

- [ ] apps/web/.env.example 파일 검토 및 업데이트
- [ ] apps/web/.env.local.example 파일 생성
- [ ] src/env.js에서 필수 환경 변수 확인
- [ ] README에 환경 변수 설정 가이드 추가

### ⚠️ Phase 4: Vercel 설정 준비 (중요)

- [ ] Vercel 환경 변수 목록 문서화
- [ ] vercel.json 파일 필요 여부 확인
- [ ] 모노레포 빌드 설정 확인

### 🔧 Phase 5: 개발 워크플로우 정리 (선택)

- [ ] 루트 README.md 업데이트
- [ ] apps/web/README.md 업데이트
- [ ] .gitignore 검토 및 업데이트
- [ ] 린팅/포맷팅 명령어 통합

### 📝 Phase 6: 문서화 (선택)

- [ ] 기술 문서 위치 정리
- [ ] Vercel 배포 가이드 작성 (apps/web/docs/)

---

## 5. 사용자 결정 필요 사항 ⚠️

### 5.1 포트 충돌 해결

**현재 상황**:

- apps/docs: 포트 3001
- apps/web: 포트 3000 (기본)

**결정 필요**:

- [ ] apps/web의 개발 포트를 변경할 것인가? (예: 3002)
- [ ] 또는 apps/docs와 apps/web을 동시에 실행하지 않을 것인가?

**권장 사항**: apps/web을 포트 3000으로 유지하고, 필요 시 개별 실행

### 5.2 공유 패키지 활용 범위

**현재 상황**:

- apps/web은 독립적인 ESLint, TypeScript, UI 컴포넌트 보유
- packages/에 공유 설정 존재

**결정 필요**:

- [ ] TypeScript 설정을 packages/typescript-config를 extends할 것인가?
- [ ] 또는 apps/web 설정을 완전히 독립 유지할 것인가?

**권장 사항**: 독립 유지 (apps/web의 복잡한 설정 고려)

### 5.3 테스트 실행 전략

**현재 상황**:

- apps/web에 Vitest 설정 존재
- 루트에서 `pnpm test` 실행 시 모든 테스트 실행 가능

**결정 필요**:

- [ ] 루트에서 모든 앱의 테스트를 한번에 실행할 것인가?
- [ ] 또는 개별 앱에서만 테스트를 실행할 것인가?

**권장 사항**: 루트에서 `pnpm test` 실행 시 모든 테스트 실행

---

## 6. 리스크 및 완화 방안

### 6.1 버전 호환성

- **리스크**: React 19, Tailwind CSS 4.0 beta
- **완화**: 의존성 버전 고정, 정기적인 호환성 테스트

### 6.2 빌드 성능

- **리스크**: 많은 의존성으로 인한 빌드 시간 증가
- **완화**: Turbo 캐싱, 원격 캐시 사용

### 6.3 Vercel 모노레포 빌드

- **리스크**: Vercel에서 모노레포 빌드 실패 가능성
- **완화**: Root Directory 명확히 설정, 빌드 명령어 검증

---

## 7. 다음 단계

1. ✅ 이 계획 검토 및 결정 사항 확인
2. ⏭️ **작업 시작**: `apps/web/tasks/web-monorepo-integration-todo.md` 참조
3. ⏭️ Phase 1-2 완료 후 로컬 테스트
4. ⏭️ Phase 3-4 완료 후 Vercel 배포 테스트

---

## 8. 참고 자료

- [Turborepo 공식 문서](https://turbo.build/repo/docs)
- [pnpm Workspace 문서](https://pnpm.io/workspaces)
- [Vercel Monorepo 가이드](https://vercel.com/docs/monorepos)
- [Next.js Deployment 문서](https://nextjs.org/docs/deployment)

---

## 변경 이력

| 날짜       | 작성자 | 변경 내용                                 |
| ---------- | ------ | ----------------------------------------- |
| 2025-11-01 | Claude | 초안 작성 - Lambda 제외, Vercel 배포 분리 |
